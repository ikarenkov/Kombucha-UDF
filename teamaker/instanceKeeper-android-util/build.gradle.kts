androidUtil(
    packageName = "ru.ikarenkov.teamaker.instancekeeper",
    owner = Teams.core,
    dependencies = deps(
        androidx.fragment,
        utils.instanceKeeper,
    ) +
        tea.core,
)